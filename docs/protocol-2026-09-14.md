# 2026-09-14 — Interface authentication

> **Status: not yet manually reviewed.** Written by an agent; awaiting human review.

The user asked for actual interface authentication, suggesting ready-made
components. Read AWS Auth guidance and checked official Werkzeug documentation
and BSD-3-Clause licensing before adding Werkzeug 3.1.8 / MarkupSafe 3.0.3.
Used the packaged scrypt hashing APIs; authored account/session integration.
No Cognito resources or other cloud services were activated.

Implemented a responsive EN/DE/IT login page, show/hide password, generic sign-in
errors and logout. Data APIs and invoice HTML require an opaque server-side
session. Passwords are stored as salted scrypt hashes, session tokens as hashes,
with eight-hour expiry, HttpOnly/SameSite=Strict cookies, Secure on HTTPS judge
hosting, and revocation on logout or password reset. Sign-in attempts are reserved
transactionally and limited per username and globally. API actor values now come
from the verified account; client-supplied names are ignored. Display-name changes
through the password reset command are rejected to preserve existing ownership.

Created the initial local account `marianna` with display name Marianna. Its random
password is in results/local-login.txt, mode 0600, ignored by Git; it was not
printed or copied to documentation. Existing invoice data was preserved. The
running local server was restarted and serves http://localhost:8765/login.
manage_users.py supports trusted-terminal creation/password reset. There is no
self-registration, MFA or recovery-email workflow; managed cloud identity remains
a future option. Local HTTP remains bound to loopback.

Replaced the prepared judge adapter's Basic Auth popup with the same login and
session flow. It forwards cookies correctly for Lambda HTTP v2 and issues Secure
cookies. The generated judge password still comes from the prepared Secrets
Manager resource. Updated the local ZIP with authentication code/assets and
dependencies; isolated imports passed. No deployment or remote-auth claim.

Verification: 52 unit/integration tests passed, including anonymous API rejection,
forged actor prevention, session expiry/reset/logout, cookie rotation, login
limits and authenticated intake with an explicitly mocked model. Browser tests
passed desktop/mobile login, EN/DE/IT, password visibility, wrong credentials,
session persistence, authenticated operator identity and logout. Existing language
browser checks and offline judge HTTP PDF/EML workflow passed. No JS errors;
syntax and git diff checks passed. Screenshots are results/auth-login-desktop.png
and results/auth-login-mobile.png; inspected both.

The existing live model evidence remains from the previous milestone. No paid
model calls were made for this authentication task. The old video predates the
login screen and is labeled accordingly. Updated README, submission wording,
review page and judge-access instructions. Repository remains private and
unlicensed for redistribution, following the user's decision to review before
publication/deployment.

## Authorized submission release

The user confirmed successful interface login, explicitly authorized all three
steps (MIT for original code, public repository, protected AWS demo in eu-north-1),
and confirmed AWS Builder ID, adulthood and absence of excluded affiliations.
This supersedes the earlier publication hold. Applied the root MIT license;
third-party licenses remain unchanged.

A video retake exposed omitted guest information despite a matching lookup.
Added structured-output validation and a regression test. All 53 tests passed.
The following retake completed real Bedrock intake, authenticated human
confirmation, ownership, synthetic PDF/EML matching and archival without JS errors.
The current video includes login. Infrastructure passed cfn-lint and project
cfn-guard checks; AWS artifact stack creation is underway, not yet a runtime claim.

Public repository created: https://github.com/mari-dev/hotel-handoff-lab; GitHub reports PUBLIC and MIT.
Published a controlled source snapshot from results/public-release, excluding
private notes, credentials, results and private Git history. Both CloudFormation
stacks completed. Remote HTTPS checks passed real Strands intake and the
authenticated synthetic workflow. Demo URL: https://whaottbycymae2xz24quiol5l40rogle.lambda-url.eu-north-1.on.aws.
Generated judge password stored only in ignored results/judge-login.txt, mode0600.
No credentials printed or committed. User must upload the public video through
their own YouTube/Vimeo account and finish the authenticated Devpost entry.

The first remote browser check found missing JS dependencies: parallel asset
requests conflicted with reserved concurrency one. The adapter now includes
reviewed local CSS/JS in its HTML response, preserving script order after the
body; application API calls are serialized. All 53 tests and the offline
adapter workflow pass. An updated cloud package is being deployed.

Cloud update completed. The final deployed Chromium check passed sign-in,
all page assets, verified Judge identity and polling, with no JS errors or
failed HTTP responses. Screenshot: results/remote-browser.png.
Public repository release commit before final docs: 0b5c894.
Video is 59.60 seconds. Devpost has not been submitted; awaiting the user's
public YouTube/Vimeo URL and authenticated form completion.
