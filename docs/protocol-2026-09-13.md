# 2026-09-13 — Private project foundation

> **Status: not yet manually reviewed.** Written by an agent; awaiting human review.

The user authorized a separate private test repository, built standalone and
autonomously, with no source copying.
Created a structural source-citation gate and synthetic regression tests.
Strands integration, semantic validation, workflow logic, UI, and live evaluation
remain pending. Verification output is stored in initial-checks.txt after execution.

## First local handoff workflow

Checked contest rules and official Strands API documentation. Added synthetic
German/Italian notes, source-backed observations, conservative conflict handling,
HTML/JSON reporting, and optional Strands extraction.
Live model evaluation remains pending. Checks are recorded in milestone-checks.txt.
SDK references: https://strandsagents.com/docs/api/python/strands.agent.agent/

Installed dependencies in a project-local virtual environment and locked them
in uv.lock. Re-ran 11 tests and generated the three-task HTML/JSON report.
Verified SDK imports, structured-output API availability, fixture schema, and
missing-configuration failure without calling a model. The user has no AWS
account or Builder ID yet. Actual model behavior remains unverified.

## 18:54 CEST — First live Strands result

The user ran demo.py --live in WSL with a temporary Bedrock API key,
AWS_REGION=eu-north-1 and HANDOFF_MODEL_ID=eu.amazon.nova-lite-v1:0.
The agent independently read results/first-live/handoff.json: four task groups.
Invoice missing information, heating conflict, and taxi request are present.
The fourth group is an explicitly completed check-in, not an invented task.
The heating failure observation is classified as requested rather than open.
This is one successful live sample, not a reliability evaluation.
Next: separate completed reports from actionable work and clarify status semantics.
The live result contains only synthetic records. No credentials were read or copied.

## Invoice coordination UI

Implemented local SQLite request storage, atomic ownership, immediate preparation,
Lexware opening, synthetic PDF/email events, and Italian responsive UI.
No paid model calls. Unknown .mcp.json left untouched.
The UI and live Strands extraction are currently separate paths. Real shared-folder
monitoring, Thunderbird access, authentication, and live extraction into the board
remain pending.

Initial UI tests exposed a SQL placeholder mismatch (10 placeholders for 9
columns); corrected it. Subsequent checks exposed unclosed SQLite connections;
added explicit connection lifetime management. All 16 regression tests now pass.
JavaScript syntax check and git diff --check pass.

## 19:53 CEST — Browser verification

Playwright/Chromium verified duplicate-name suggestions, atomic claim and Lexware
popup navigation (external page intercepted), simulated PDF/email progression,
history, new requests, and mobile layout without horizontal overflow. No JavaScript
errors. Test used a disposable database, not the running app database.
Chromium initially lacked libasound.so.2; extracted an Ubuntu package into /tmp
for the browser test without system installation. Desktop/mobile screenshots
were inspected. Replaced a missing plus glyph with ASCII and made mobile
navigation labels visible. Local app runs in detached session PID 50144 on
http://localhost:8765; GET /api/requests returned HTTP 200.

## Local evidence automation and AI intake

Added actual PDF parsing and SHA-256 attachment matching for exported Sent EML.
Name plus exact stay text must match uniquely; ambiguity and revisions remain
visible. Removed direct simulated state-transition API actions. Lab buttons now
create real synthetic files only; an independent monitor updates statuses.
Added source-backed Strands intake proposals, bounded to five model turns and
20 attempted analyses per UTC day. These are workload controls, not a monetary
cap. New intake schema live evaluation remains pending.

## 20:30 CEST — local demo packaging

Added a credential-inheriting local launcher, updated README to distinguish real file monitoring from integrations, and prepared English demo/submission drafts. Final invoice-specific live AI verification remains pending. No source repository was modified.

## English interface

Translated interface labels, accessibility text, errors, new system events, and new synthetic seed copy to English. Legacy source values remain API-compatible. Historical system events are translated at display time; existing guest notes and records are preserved.

### English-only follow-up

At the user’s explicit request, removed legacy Italian UI mappings, converted source values and existing synthetic request/event text to English, and changed new sample PDFs to INVOICE. Backed up the local database before migration; original evidence files and attachment hashes remain intact.

## Optional interface languages

Added local i18next catalogs for English (default), German, and Italian with browser-persisted selection. Translation targets explicit interface labels and system messages, never guest input or source evidence. No AI or remote translation service is used.

### Language verification

Fresh validation: 24 unit tests passed; the Playwright language check passed for all three languages, persisted preference, unsaved form fields, stable English API source values, localized history/errors, unchanged guest notes, and mobile width. No JavaScript errors. Checked the mobile screenshot.

## AWS Agent Toolkit setup verification

Installed official AWS CLI 2.36.44 after the user installed unzip; installer verified the GPG signature. User completed browser login for profile hotel-handoff-lab. STS identity verified for the intended project; selected Region eu-north-1. The toolkit catalog returned 104 skills. The wizard could not find Codex on the user terminal PATH; resolved this using the existing VS Code extension binary and `codex mcp add`, without another Codex installation. Added only the aws-mcp entry to the global Codex configuration, using mcp-proxy-for-aws 1.6.4, explicit AWS_MCP_PROXY_PROFILES, and eu-north-1 operation metadata. A real MCP client initialized successfully and listed 8 tools. No model invocations, resource creation, or deployment performed. New session required for tool discovery.

## 23:05 CEST — Profile authentication and invoice live checks

Agent-written update; awaiting human review. Added explicit AWS_PROFILE support
to intake, health configuration and launcher, plus boto3[crt] for aws login.
Regional Nova Lite was verified ACTIVE and invoked through the actual HTTP
endpoint with synthetic data. The configured AWS MCP tools were not exposed in
this session; read-only AWS verification used the CLI instead.

The live acceptance check remains FAILED after five first-case iterations;
see live-invoice-status.md. Tightened nullable address guidance and literal
quotations, and now reject proposals that omit source reading or duplicate lookup.
Added a reproducible disposable-database HTTP smoke test and profile unit tests.

Updated README, current architecture, submission caveats and readiness checklist.
The user confirmed German residence and Italian nationality; age and excluded
affiliations remain unconfirmed. Public licensing, video, judge access and final
submission remain outstanding. No resources were created or content published.

Final verification: 27 unit tests passed; JavaScript syntax and git diff --check
passed. Live acceptance remains failed and is explicitly distinguished from
these offline checks.

## 23:30 CEST — Live workflow complete; materials awaiting user review

Agent-written update; awaiting human review. The user asked to finish today.
Resolved the extraction failures by switching model output to flat literal
strings, converting empty values to nullable public fields, and running source
and tool-execution validation inside the Strands structured-output tool. Original
post-extraction validation remains active. The five-turn limits were preserved.

Final actual HTTP run: four correct live proposal cases (English missing address,
no invoice request, Italian request, German full address). A fifth adversarial
case is safely rejected with HTTP 502 after bounded validation failure; this is
not counted as successful extraction. Added conservative EN/DE/IT demand checking
after discovering that a literal breakfast quote could otherwise support a false
invoice flag. This narrow gate can reject valid unusual phrasing; no general
semantic or prompt-injection robustness claim is made.

A real Playwright browser run completed paid Strands intake, explicit creation,
claim, actual synthetic PDF and EML file reconciliation, and archival. Lexware
navigation was intercepted and disclosed. No email sent. Video: 55.72 seconds,
results/demo/hotel-handoff-live-demo.webm; four screenshots and browser evidence
are alongside it. Live report snapshots are retained under docs. Forty unit
tests, JavaScript syntax, git diff checks and EN/DE/IT browser checks passed.

Prepared judge_lambda.py, infra/judge-demo.yaml and a 33 MB Linux Python 3.13 ZIP
in results/judge-demo.zip. Password-protected routes, regional inference only,
single reserved concurrency, generated Secrets Manager password and seven-day
logs. Temporary warm-instance SQLite storage is explicitly a demo limitation.
The adapter passed offline HTTP integration, the bundle passed isolated imports,
and cfn-lint passed. Cloud deployment remains unverified and has NOT occurred.
The artifact bucket and deployed access still need creation after approval.

Prepared the English Devpost draft, architecture.svg, package license inventory,
judge-access plan and results/review.html with embedded video and entry text.
The user confirmed Devpost registration but has not created the project entry.
AWS Builder ID, adult status and excluded affiliations remain unconfirmed.

The user explicitly chose to review the materials before authorizing MIT,
publication or AWS demo activation. Respect that decision: repository remains
private/local; no root redistribution license assigned, no deployment, no video
upload and no submission. Next: user review, requested edits, then explicit
authorization and final remote verification/publication steps.
Pre-existing AGENTS.md changes were preserved.

Authentication work continued across midnight; see protocol-2026-09-14.md.
